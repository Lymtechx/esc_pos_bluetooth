name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
publish_to: none
version: 1.0.1
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0

  esc_pos_utils:
    git:
      url: https://github.com/lymtechx/esc_pos_utils.git
      ref: 2.0.1
      
  flutter_bluetooth_basic:
    git:
      url: https://github.com/lymtechx/flutter_bluetooth_basic.git
      ref: 2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
